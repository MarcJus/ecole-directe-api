"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Family = void 0;
const Account_1 = require("./Account");
const types_1 = require("../types");
const functions_1 = require("../functions");
class Family extends Account_1.Account {
    constructor(session) {
        super(session);
        this.session = session;
        this.type = "family";
        const mainAccount = functions_1.getMainAccount(session.loginRes.data.accounts);
        if (!types_1.isFamilyAccount(mainAccount))
            throw new Error("Family class's main account is wrong");
        if (!session.token)
            throw new Error("Account class MUST have token");
        this.account = mainAccount;
        this.token = session.token;
    }
    get _raw() {
        return this.account;
    }
}
exports.Family = Family;
