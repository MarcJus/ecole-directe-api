import { mailboxResSuccess } from "ecoledirecte-api-types/v3";
import { Message } from "../../classes";
import { Student } from "../../accounts";
export declare function getMessages(id: number, token: string, direction?: "received" | "sent", context?: Record<string, unknown>): Promise<mailboxResSuccess>;
export declare function cleanMessages(mailboxRes: mailboxResSuccess, student: Student): Message[];
