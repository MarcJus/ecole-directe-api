/// <reference types="node" />
import { Account } from "./Account";
import { Session } from "../Session";
import { staffAccount } from "ecoledirecte-api-types/v3";
export declare class Staff extends Account {
    private session;
    type: "staff";
    private account;
    constructor(session: Session);
    private _photo?;
    private _photoUri?;
    getPhoto(): Promise<Buffer | undefined>;
    get photo(): {
        buffer?: Buffer;
        uri?: string;
    };
    get _raw(): staffAccount;
}
