/// <reference types="node" />
import { Account } from "./Account";
import { Session } from "../Session";
import { teacherAccount } from "ecoledirecte-api-types/v3";
export declare class Teacher extends Account {
    private session;
    type: "teacher";
    private account;
    constructor(session: Session);
    private _photo?;
    private _photoUri?;
    getPhoto(): Promise<Buffer | undefined>;
    get photo(): {
        buffer?: Buffer;
        uri?: string;
    };
    get _raw(): teacherAccount;
}
